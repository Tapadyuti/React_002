import React from 'react'

function Columns() {
    return (
        <React.Fragment> {/* only allowed to be passed is key */}
            <td>Name</td>
            <td>RS</td>
        </React.Fragment>
    )
}

export default Columns
